// Judge dimensions for the `live-browser` scene.
//
// Empty for v1. The judge surface today is film-wide; a `live-browser`
// scene is graded by the same rubric as every other scene type. A future
// dimension worth landing: "the captured session actually shows the move
// the narration claims" — a model graded against the captured MP4's first
// + last frame. Pairs with the (future) depth rule about action progress.

import type {JudgeDimension} from '@bjelser/kit';

export const judgeDimensions: ReadonlyArray<JudgeDimension> = [];

export default judgeDimensions;
